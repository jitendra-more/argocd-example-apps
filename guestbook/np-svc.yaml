apiVersion: v1
kind: Service
metadata:
  creationTimestamp: null
  labels:
    app: guestbook-ui
  name: guestbook-ui
spec:
  ports:
  - name: "32000"
    NodePort: 32000
    protocol: TCP
    targetPort: 80
  selector:
    app: guestbook-ui
  type: NodePort
